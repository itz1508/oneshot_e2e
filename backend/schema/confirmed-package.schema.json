{
  "$id": "urn:oneshot:schema:confirmed-package:2",
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "additionalProperties": false,
  "properties": {
    "confirmed": {
      "const": true,
      "type": "boolean"
    },
    "core": {
      "additionalProperties": false,
      "properties": {
        "audit": {
          "$ref": "urn:oneshot:schema:audit:2"
        },
        "evaluation": {
          "$ref": "urn:oneshot:schema:evaluation:2"
        },
        "fixture": {
          "$ref": "urn:oneshot:schema:fixture:2"
        },
        "gap_analysis": {
          "$ref": "urn:oneshot:schema:gap:2"
        },
        "goal": {
          "$ref": "urn:oneshot:schema:goal:2"
        },
        "plan": {
          "$ref": "urn:oneshot:schema:plan:2"
        },
        "researcher": {
          "$ref": "urn:oneshot:schema:researcher:2"
        },
        "schema_artifact": {
          "$ref": "urn:oneshot:schema:schema-artifact:2"
        },
        "triple_validation": {
          "$ref": "urn:oneshot:schema:triple-validation:2"
        },
        "validation": {
          "$ref": "urn:oneshot:schema:validation:2"
        }
      },
      "required": [
        "researcher",
        "plan",
        "schema_artifact",
        "fixture",
        "goal",
        "validation",
        "audit",
        "gap_analysis",
        "evaluation",
        "triple_validation"
      ],
      "type": "object"
    }
  },
  "required": [
    "confirmed",
    "core"
  ],
  "type": "object"
}
