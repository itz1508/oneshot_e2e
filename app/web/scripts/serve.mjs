import http from'node:http';import fs from'node:fs';import path from'node:path';const root=path.resolve('dist'),types={'.html':'text/html','.css':'text/css','.js':'text/javascript'};http.createServer((req,res)=>{const p=path.join(root,req.url==='/'?'index.html':req.url.split('?')[0]);if(!p.startsWith(root)||!fs.existsSync(p)){res.writeHead(404);return res.end('not found')}res.setHeader('content-type',types[path.extname(p)]||'text/plain');fs.createReadStream(p).pipe(res)}).listen(4173,'127.0.0.1',()=>console.log('http://127.0.0.1:4173'))