OneShot Web Application — Master Source of Truth v3.0Status: Authoritative product / Builder source of truth  Mode: PRESERVE + CORRECT  Date: 2026-09-07  Supersedes: `OneShot Web Application — Builder Source of Truth v2.0` and behavioral/layout assumptions in `oneshot_v7.html`---0. Authority and Source PrecedenceThis document defines the current OneShot web application behavior, layout, runtime/UI boundaries, human gates, conversation behavior, Task Management, Job History, Explorer behavior, Build/Sandbox presentation, and implementation constraints.When sources disagree, use this precedence:Canonical OneShot workflow — fixed stage order, ownership, human gates, deterministic validation/hash rules.Latest user-established application behavior in this document — especially the five-region shell, conversation center, rail ownership, and right-side flip meaning.Current repository/runtime evidence — actual routes, stores, queue behavior, Sandbox contracts, run state, events, artifacts.Static HTML/design references — visual language only. They are never authoritative for workflow, state ownership, data, or layout ownership.`oneshot_v7.html` is not a runtime/data source and is not the authoritative layout contract. It may be used only for visual tokens such as spacing, typography, radius, borders, density, icon treatment, and motion feel.If a visual reference conflicts with this document, this document wins.---1. Non-Negotiable Preserve RulesThe Builder must not:rename existing IDs;change which stage produces an ID;change stage ownership;insert or remove workflow stages;reinterpret an existing ID into another artifact;create a second run-level Job ID;start Planner before Research Review is explicitly accepted;start Builder before Build Ready is explicitly confirmed;silently reuse an invalidated confirmation hash;erase prior Job/run history because current work was superseded;treat OneShot's own source as the selected target project unless explicitly selected;invent another persistence system, queue, database, deployment platform, or auth flow;invent frontend routes merely for convenience;simulate successful Research, validation, Build, Sandbox, mutation, or hash state;expose hidden/private model chain-of-thought;make the frontend the canonical run/Plan/task store;hardcode representative UI values and present them as live runtime state;use timer-based fake progress as runtime truth;add cleanup/refactor work that is unrelated to the approved implementation scope.If an actual repository contract is missing, report the gap. Do not fill it with a mock and call it production-ready.---2. Canonical WorkflowThe accepted successful path remains:```textUser Prompt   ↓1. Researcher                      ← LLM #1   ↓STOP — Research Review             ← HUMAN GATE #1   ├─ edit Research sections   ├─ request more Research   └─ ACCEPT        ↓Accepted Research Baseline        ↓2. Planner                         ← LLM #2        ↓3. Refactor                        ← LLM #3        ↓4. Gap Analysis                    ← LLM #4        ↓5. Evaluation                      ← LLM #5        ↓Triple Validation                  ← DETERMINISTIC   ├─ Schema VALID   ├─ Fixture VALID   └─ Goal VALID        ↓CONFIRMED        ↓Create confirmation hash           ← DETERMINISTIC        ↓STOP — BUILD READY                 ← HUMAN GATE #2   ├─ Cancel / return   └─ CONFIRM BUILD        ↓6. Builder                         ← LLM #6        ↓Sandbox execution                  ← CODE / RUNTIME        ↓Post-build validation/hash check   ← DETERMINISTIC        ↓DONE```Normal successful accepted path = 6 LLM calls.Research Again adds another Researcher call.  Deterministic validators and hash checks do not become LLM calls.No additional mandatory human gate may be inserted into the normal workflow.---3. Hash and Builder Handoff RuleThe confirmation package handed to Builder is the exact confirmed immutable package together with its confirmation hash.Do not:create another pre-build package;reinterpret the confirmed Plan;widen scope;recompute a different confirmation representation;create a second Build-only run.Post-build verification remains:```texthash == hash_sandbox```The two hashes are computed from the same established canonical comparable representation.Equality is deterministic. An LLM does not decide it.---4. Runtime OwnershipThe current runtime remains authoritative.Existing ownership includes:```textConversationStore    ↓IntentCollectionServiceAppendOnlyProcessingEventStore    ↓ProcessingEventBus    ↓TaskManagement    +CheckpointStoreRunRepositoryFileArtifactStorePipelineHistoryBullMQ / Redis pipelinelegacy inline fallback runtimeSandboxService    ├─ HardenedProcessRunner    └─ ContainerSandboxRunner```The browser consumes/projects this state.The browser must not become a second independent workflow store.---5. Run IdentityUI term:```textJobId = run_id```This is only a display alias.Do not create:```textjob_id != run_id```for the same OneShot execution.Artifact IDs remain separate and retain existing ownership:prompt_idresearch_idplan_idaudit_idschema_idfixture_idgoal_idbuild_idartifact IDsother established IDs---6. Known Browser ContractsPreserve existing contracts, including:```textGET  /api/healthPOST /api/conversationsPOST /api/conversations/:id/messagesPOST /api/conversations/:id/promptPOST /api/conversations/:id/runGET  /api/runs/:idGET  /api/runs/:id/events             SSEGET  /v1/workspace/tree?path=.&depth=3GET  /v1/workspace/file?path=...GET  /api/runs/:id/artifacts/:name```Current implementation also uses the existing run/action contracts for Research Review, Build Ready, and the approved Research Again routing. Inspect the current server before changing any route.Do not replace SSE with polling for live state already provided by SSE/run snapshots.---7. Current Implementation Status — 2026-09-07The following approved batch is implemented:7.1 ImplementedResearch Again on the same `run_id`;Research revision separated semantically from downstream iterative refinement counters;versioned Research bundle history;latest Research baseline used for confirmation;active-run normal conversation messages;`normal` active-run messages do not mutate Plan/pipeline state;Research Again mode through the main composer;HardenedProcessRunner pre/post workspace mutation classification;real created / modified / deleted evidence;SHA-256 mutation evidence where available;`file-mutations` artifact derived from Build evidence;Job History detail consumes the `file-mutations` artifact;backend/API flow verified for Research Again;typecheck/build/frontend tests passed in the implementation report.7.2 Still required to close that batchA real browser/E2E proof is still required for:```textPrompt→ Research→ Research Review→ Research Again→ instruction through same composer→ Research rerun on same run_id→ new Research Review→ Accept→ Planner starts once```Also verify in-browser:active-run normal chat;real file-changing Sandbox run;mutation artifact → HTTP → Job History rendering;final `hash == hash_sandbox`;no regression to Research Review or Build Ready.7.3 Explicitly deferred / not approved for implementation yetfull global post-Planner Plan Edit;Phase Edit;Task/Step Edit;optional safe-boundary edit interrupt architecture;full ContainerSandboxRunner execution rebuild;new Conversation↔Run persistence/index model.These remain planning-only until separately approved.---8. Frontend Framework DirectionThe intended migration target is:```textNext.js 16.3.xApp RouterReactTypeScriptTailwind CSS 4```The frontend remains separate from the existing OneShot Node.js backend.Target relationship:```textNext.js frontend      ↓ HTTP / SSEexisting OneShot Node.js backend      ↓BullMQ      ↓Redis      ↓Worker      ↓Sandbox/runtime```Do not:keep Vite and Next.js as competing production frontends after migration;move BullMQ/Redis/worker/Sandbox into Next.js Route Handlers;install queue/database runtime packages into browser code;create a second backend merely for the frontend migration.Until migration is complete, the current frontend implementation remains authoritative for actual working code.---9. Authoritative Application ShellThe active application uses five structural vertical regions, plus a compact top bar:```text┌────────────────────────────────────────────────────────────────────────────────┐│ TOP BAR                                                                        │├──────┬────────────────────┬────────────────────────────────┬──────────────────┬──────┤│ LEFT │                    │                                │                  │RIGHT ││ RAIL │ EXPLORER           │ CONVERSATION                   │ TASK MANAGEMENT  │ RAIL ││      │                    │                                │ / JOB HISTORY    │      ││      │ target workspace   │ Prompt → Response              │                  │      ││      │ files/folders      │                                │ Current Job      │      ││      │                    │ inline Research Review         │                  │      ││      │                    │ inline Plan/validation         │ Phase / Step     │      ││      │                    │ inline Build Ready             │ Task only if real│      ││      │                    │ inline Sandbox                 │                  │      ││      │                    │ inline Final Result            │  ↕ 180° flip     │      ││      │                    │                                │ Job History      │      ││      │                    ├────────────────────────────────┤                  │      ││      │                    │ PromptBar — persistent composer│                  │      │└──────┴────────────────────┴────────────────────────────────┴──────────────────┴──────┘```Structural ownership is fixed:Left RailExplorerCenter ConversationTask Management / Job HistoryRight RailThe center is the dominant surface.Approximate desktop proportions may start around:```textLeft Rail          44–56 pxExplorer           240–300 pxCenter             flex: 1Task Management    300–360 pxRight Rail         44–56 px```Exact pixel values are implementation details.---10. Top BarThe top bar is compact application chrome.It may show real:OneShot identity;selected target/workspace;current `run_id` / JobId when active;connection/backend health;provider/config entry where supported;minimal global controls such as New Job.Do not turn the header into a large pipeline dashboard.Internal processors may be shown in advanced/debug/history detail, but they must not dominate the normal product experience.---11. Left RailThe Left Rail is a narrow persistent navigation/control strip.It is not the Explorer itself.Its purpose is to select/focus left-side application surfaces such as:Explorer/workspace;search, only if implemented;other approved project-navigation tools.Rules:icon-first;low visual weight;clear active state;tooltip on hover;no large file tree inside the rail;no run activity feed inside the rail;no 180° flip;no automatic flip when a run starts.The adjacent Explorer remains the working file panel.---12. ExplorerExplorer is stable and persistent.It shows the complete selected target workspace, not only run artifacts.Use the real workspace APIs.It must support real:loading;empty;error;folder expansion;file selection;selected target identity;file content opening;mutation decoration when runtime evidence exists.It may decorate real files as:CreatedModifiedDeleted only when actually represented by evidencebut mutation state does not generate the tree.Do not hardcode:repository folders;file names;counts;timestamps;mutation badges;success states.12.1 No Explorer flipThe Explorer must not flip to:activity;running status;precreated files;pipeline state;history.The old `Explorer ↔ Activity` / auto-flip concept is superseded.---13. File ViewerOpening a file:reads the actual selected workspace path;shows actual file content;does not mutate the file merely because it was opened;may show real loading/error state;may show actual before/after/diff/hash metadata when runtime evidence exists.Do not fabricate before/after state or hashes.---14. Center = Continuous Prompt/Response ConversationThe center is a continuous chronological conversation, not a separate permanent work dashboard plus chat.The product reads as:```textUser Prompt↓OneShot Response↓structured response/card when applicable↓User Prompt / decision↓OneShot Response↓structured response/card...```The conversation is the primary communication/work surface.Do not create:Chat vs Plan tabs;a permanent Plan workspace above chat;a separate Research Review page;a separate Build page;a separate bottom Build/Sandbox sheet;a generic dashboard replacing conversation;a static card stack representing future states.---15. Chat Rendering and Message GroupingRender the actual chat/conversation state as the transcript.Do not hardcode example user/assistant messages into production markup.15.1 Assistant grouping ruleGroup related runtime cards into one coherent assistant response when they belong to the same logical OneShot turn.Example:```textUserFix the validation path while preserving legacy behavior.OneShotI finished researching the selected target. The existing path coversschema validation, but the regression fixture is incomplete.[Research Review card]```Another example:```textOneShotThe confirmed Plan passed all required deterministic validation.[Build Ready card]```And:```textOneShotBuild execution completed and verification passed.[Sandbox result][Final Result]```Do not create a separate assistant bubble for every internal event.Fine-grained runtime events belong in Task Management / Job History.15.2 Response boundariesUse semantic boundaries:new user prompt → new response boundary;human hook requiring user action → clear response boundary;terminal result → final response boundary;related status/cards for one logical response may remain visually grouped.Do not group by arbitrary timer windows.---16. PromptBar`PromptBar` is the actual persistent composer at the bottom of the center pane.It must:use the actual conversation API;remain available during normal active-run chat when runtime behavior allows it;support sending/loading/error state;preserve draft naturally;remain associated with the active conversation and `run_id`;support approved Research Again mode.It must not be decorative.It must not move into a global bottom sheet.---17. Normal Active-Run ChatDuring normal execution, ordinary chat may continue.An ordinary active-run message:is added to the conversation;may emit user-visible support/history evidence associated with the current run;does not mutate the accepted Plan;does not restart Planner;does not change Phase/Task authority;does not authorize Build;does not invalidate or recreate the confirmation hash by itself.Normal chat is communication, not mutation authority.---18. Research Review — Human Gate #1Research completion is a hard stop.Required behavior:```textResearcher completes        ↓Task Management → Waiting / Human Hook        ↓OneShot response appears in center        ↓[Research Review card]        ↓Research Again / appropriate edits / Accept```Planner must not start before explicit acceptance.The Research Review card shows only real Research sections that exist, for example:Current User Intent;Requirements;Evidence / Findings;Goals;Success Criteria;Fixtures / Test Cases;Constraints;Design / Implementation Direction.Research is the baseline, not the final Plan.---19. Research AgainResearch Again is implemented as a same-`run_id` Research revision.Required model:```textResearch revision 0→ Research Review→ Research Again→ Research revision 1→ new Research result→ new Research Review→ Accept→ Planner```Rules:same `run_id`;previous Research retained as history;latest Research becomes the active review baseline;Planner remains blocked until the latest review is accepted;no manual deletion of prior checkpoint/history evidence;Research revision is semantically distinct from downstream refinement-loop iteration.19.1 Research Again UXDo not add a second composer inside the review card.`[Research Again]` switches the main PromptBar into Research Again mode.The next user instruction is:stored as conversation input;associated with the current run;routed through the approved Research Again backend behavior;followed by Researcher rerun.---20. Active Plan / Validation ConversationAfter Research acceptance, downstream work continues in the same conversation.Do not replace the center with a permanent Plan dashboard.OneShot may append/update coherent assistant responses containing actual available:Plan Summary;accepted scope;requirements;affected files;constraints;validation state;revision metadata;user-facing progress summaries.The working state remains runtime-owned.The conversation remains the human-readable communication/history view.---21. Build Ready — Human Gate #2After deterministic Triple Validation and confirmation hash creation:```textSchema VALIDFixture VALIDGoal VALIDPlan/package CONFIRMEDconfirmation hash created        ↓STOP — BUILD READY```The Build Ready state appears as an inline assistant response/card in the center conversation.Show actual:validation results;confirmed package/scope summary;confirmation hash;Confirm Build;Cancel / Return.Do not create a separate full-width Build sheet.Do not start Builder until explicit Confirm Build.---22. Build Ready Cancel / ReturnCancel / Return is non-destructive.It must:not start Builder;not mutate the target workspace;not create another run;preserve the current confirmed package/hash while still valid;return the center conversation to the current confirmed Plan/build-ready context.If a later authorized change modifies the confirmed package, the prior confirmation/hash becomes stale and must not authorize the changed package.---23. Sandbox PresentationThe real Sandbox runtime remains:```textSandboxService    ├─ HardenedProcessRunner    └─ ContainerSandboxRunner```For the current working MVP, HardenedProcessRunner is the proven real mutation-evidence path.Do not rebuild or claim full ContainerSandboxRunner capability unless separately approved and implemented.23.1 Sandbox UI ownershipSandbox is an inline structured assistant response/card, not:a full-width bottom sheet;a fake terminal;an always-visible panel;a timer-based animation.After Confirm Build:```textOneShotBuild confirmed. Starting isolated execution.[Sandbox card]status from real runtime```As real events/evidence arrive, update the card.If the runtime only exposes coarse states, show coarse states. Do not invent granular steps.---24. File Mutation EvidenceThe current implementation derives mutation evidence from real HardenedProcessRunner workspace snapshots.Mutation record includes real evidence where available:path;created / modified / deleted;current bytes;previous bytes;current SHA-256;previous SHA-256.Unchanged files are not mutation records.The `file-mutations` artifact is derived from Build/Sandbox evidence and exposed through the existing artifact route.Failed execution must preserve real mutation evidence when evidence exists.Do not:store fake `bytes: 0`;invent hashes;invent content references;make a frontend-only ledger canonical.---25. Final ResultA successful final result appears only after real terminal success.The center conversation shows an actual final assistant response/card with available:JobId / `run_id`;result;changed files;validation state;confirmation hash;sandbox/result hash;equality verification;final state.Verification rule remains:```texthash == hash_sandbox```A failed/root-cause execution must show the actual failure.Do not animate a fake successful ending.---26. Right Working Panel = Task ManagementThe adjacent right working panel is Task Management, not a generic tabbed Inspector.Front face:```textCurrent Job```Back face:```textJob History```This panel may be open/persistent according to the established shell behavior, but its content ownership is fixed.Do not replace it with:Planned vs Implemented;generic inspector tabs;provider configuration;activity feed;unrelated debug panels.---27. Task Management — Current JobCurrent Job shows actual current run state:JobId (`run_id`);human-readable Phase;current Step;actual Task/Todo only if real sub-step evidence exists;progress;health;hook/waiting state;relevant target/workspace context.Normal running state is read-only.Task Management is projected from:ProcessingEventBus;AppendOnlyProcessingEventStore;TaskManagement;CheckpointStore;RunRepository;PipelineHistory.No fake timer determines completion.---28. Phase / Step / Task PresentationCurrent repository evidence establishes processors and PlanStep, but does not establish a canonical sub-task schema for every Step.Therefore:```textPhase  └─ Step```is valid when no real child tasks exist.Only render:```textPhase  └─ Step      └─ Task```when real runtime/sub-step evidence exists.Do not duplicate a PlanStep and label the duplicate as a Task merely to fill the hierarchy.Human-readable phase labels may normalize internal processor names, for example:```textResearcher          → Researching projectPlanner             → Preparing workRefactor            → Refining planGap Analysis        → Checking gapsEvaluation          → EvaluatingTriple Validation   → ValidatingBuilder             → BuildingPost-build check    → Verifying```Internal names remain unchanged in backend/runtime ownership.---29. Right-Panel 180° FlipThe Y-axis 180° flip has exactly one meaning:```textCurrent Job  ↔  Job History```Front:```textCurrent JobPhaseStepTask if realprogresshealthwaiting/hook state```Back:```textJob HistoryJob list | selected Job files/evidence```Do not use the same flip for:Explorer;Research → Planner;ordinary Phase transitions;Build Ready;Sandbox;Plan cards;Planned vs Implemented.No auto-flip merely because a run starts.---30. Job HistoryJob History is real historical run evidence.Use existing `GET /api/runs` / RunRepository-derived data and existing artifact retrieval.Do not add duplicate backend aliases merely for UI convenience when the existing response already supplies:`run_id`;`pipeline_status`;`test_result`;`current_processor`;`hash_proof`;`updated_at`.Map these to human-readable UI labels in the frontend.When a Job is selected, show actual available:status/result;update time;hash proof;final verification;file mutations;evidence/history.Historical evidence must remain unchanged when current work changes.30.1 Split viewThe back face may use approximately:```text┌──────────────────────┬──────────────────────┐│ Job list             │ Selected Job files   ││                      │ / mutation evidence  ││ ~50%                 │ ~50%                 │└──────────────────────┴──────────────────────┘```---31. Right RailThe Right Rail is a separate narrow persistent application-control strip.It is not Task Management itself.It may provide compact controls/icons to:focus/open Current Job;invoke the approved Current Job ↔ Job History interaction;open provider/settings/status surfaces where those already exist.Rules:narrow;icon-first;low visual weight;clear active state;tooltips;no large operational content inside the rail;no replacement of the Task Management working panel.---32. Realistic Dynamic UI RulesThe application must feel like a live AI engineering product.Use:```textREAL DATA→ REAL STATE TRANSITIONS→ REAL LOADING→ REAL RESPONSES→ REAL HUMAN HOOKS→ REAL SANDBOX/BUILD EVIDENCE```Never:```texthardcoded DOM→ fake stages→ fake files→ fake dates→ fake completion→ fake Sandbox```Required transient states include where applicable:idle;loading;sending;running;waiting for user;success;failure;disconnected;empty.Future cards must not be pre-rendered as though their future state already exists.---33. No Frontend Heuristic ReadinessGenerate/Run readiness is runtime-owned.Do not infer readiness from:prompt length;keyword arrays;local `"intent/outcome/constraints"` heuristics;fake scores;static LED logic.The old v7-style keyword/readiness logic is not authoritative production behavior.If the runtime does not provide readiness, report the missing contract rather than inventing a heuristic.---34. No Fake Progress or Fake Chain-of-ThoughtDo not use `setInterval`/timers to manufacture workflow completion.A timer may animate a spinner, but it may not determine runtime truth.Acceptable user-facing text:```textResearching the selected project…Checking validation coverage…Waiting for Research Review…Building in the Sandbox…```Not acceptable:hidden/private chain-of-thought;simulated internal reasoning;fabricated evidence;fabricated processor results.Detailed internal events can appear in Task Management/history only when they are actual product events.---35. Target Workspace BoundaryOneShot operates on a selected target project.```textOneShot application        ↓ controlsSelected target workspace        ↓Pipeline```OneShot's own source is not silently the target.Bundled Sample and uploaded/user-selected projects must converge on the same normal workflow.A Sample is a real materialized target workspace, not a fake Explorer tree.Upload Project must not be claimed complete unless real materialization/transport exists.---36. Sample ModeOpening the bundled Sample must behave exactly like normal OneShot:```textOpen Sample→ select/materialize actual bundled target→ normal five-region OneShot shell→ Explorer shows real sample files→ center shows normal conversation→ right shows normal Task Management→ prompt submitted→ real workflow runs```No separate demo dashboard.No simulated pipeline.---37. Human Hook AuthorityRuntime state authorizes mutation.UI visibility only mirrors authority.During normal execution:Plan/Phase/Task edits are closed;Plan/work surfaces are read-only;Task Management is read-only;ordinary chat does not silently authorize replanning.At a real hook:```textRUNNING  ↓HOOK / QUESTION  ↓allowed authority opens  ↓USER RESPONSE  ↓runtime validates current revision/state  ↓transition accepted  ↓history/context synchronized  ↓controls close  ↓RUNNING```Stale or duplicate submissions must not cause duplicate transitions.---38. Plan / Phase / Task Edit StatusThere are exactly three conceptual edit scopes:Plan Edit — globalPhase Edit — phase scopeTask/Step Edit — narrow scopeHowever, full implementation remains deferred/not approved in the current implementation batch.Do not scaffold or partially wire these simply because the UI has a place for future controls.A future implementation must preserve:same `run_id`;non-destructive historical evidence;revision-aware authority;safe processor-boundary interruption;no new mandatory workflow gates.---39. Persistence and HistoryUse existing runtime ownership:RunRepository;FileArtifactStore;AppendOnlyProcessingEventStore;CheckpointStore;PipelineHistory;Redis-backed pipeline state.Do not add a new database solely for Job History or file mutation display.Deployment durability on Render remains configuration-dependent.Do not claim cross-redeploy durability unless actual persistent backing is configured and verified.---40. Authentication and Provider BoundariesAuthentication remains:same-origin browser session;optional `ONESHOT_API_TOKEN` Bearer token.Bearer token remains in `sessionStorage`.Do not invent login/CSRF routes.Provider/API credentials stay out of the frontend build.Provider labels/status shown in the UI must reflect actual backend/provider configuration, not hardcoded vendor/model strings.The static v7 provider values are design/demo content, not authoritative runtime data.---41. Visual Design LanguageThe v7 visual reference may contribute:dark surface hierarchy;restrained blue/accent treatment;compact typography;thin borders;moderate radius;subtle hover/focus states;dense engineering-tool spacing;icon sizing;smooth but restrained motion.It may not redefine:shell ownership;workflow;gate count;rail ownership;Explorer behavior;right-panel flip meaning;conversation architecture;runtime state;provider truth;file truth;readiness truth.The product should feel like an AI engineering application, not:CI dashboard;DevOps console;pipeline monitor;admin console;static prototype.---42. Superseded v7 BehaviorsThe following concepts from `oneshot_v7.html` are explicitly superseded and must not be copied into production behavior:left Explorer auto-flip to Running Status;Explorer flip control;precreated-file activity back face;right Task Management flip to Planned vs Implemented;generic right drawer tabs replacing the Current Job/Job History contract;hardcoded startup assistant transcript;local keyword-based readiness LEDs;static provider/model truth;timer-driven stage completion;hardcoded fake stage activity;sample files/dates/statuses as production state;instructions telling the user that Explorer flips during a run.Retain only useful visual styling, not those behaviors.---43. Component Ownership TargetUse component boundaries appropriate to the actual frontend framework, conceptually:```textAppShellTopBarLeftRailExplorerTreeNodeFileViewerConversationMessageAssistantResponsePromptBarResearchReviewCardPlanSummaryCardBuildReadyCardSandboxCardFinalResultCardTaskManagementCurrentJobJobHistoryRightRail```These are conceptual responsibilities, not permission to create unnecessary files.Reuse existing components/modules where appropriate.Do not create a component merely to mirror every CSS block from the static reference.---44. Completion Gate for the Current UI CorrectionBefore claiming the UI corrected, prove the rendered shell first:```textTop BarLeft RailExplorerCenter  Conversation  PromptBarRight  Current Job       ↕  Job HistoryRight Rail```Then verify:Layoutboth rails exist;Explorer is separate from Left Rail;Task Management is separate from Right Rail;Explorer does not flip;right panel flips only `Current Job ↔ Job History`;no bottom Build sheet;center dominates the layout.Conversationreal `state.chat`/conversation data renders chronologically;PromptBar is always the active composer when runtime allows;related cards group into coherent assistant responses;no prewritten fake transcript is required to make the page look populated.RuntimeResearch Review appears only at real gate;Research Again uses the same composer;normal active-run chat does not mutate Plan;Build Ready appears only after real confirmation/hash state;Sandbox uses real runtime state;Final Result requires real terminal outcome.Explorerreal selected workspace tree;real file contents;no hardcoded source tree;mutation decorations are evidence-driven.Task Management / Historyprogress from runtime events/checkpoints;JobId = run_id;no fake Task children;Job History uses real runs;file mutations load from the real artifact.VerificationRun actual repository commands appropriate to the current repo, including:```textTypeScript typechecktest build/typecheckroot buildfrontend testsfocused backend tests```Then perform a real browser walkthrough.Passing unit tests alone is not sufficient to close the visual/runtime integration gate.---45. Current Remaining Product BacklogAfter the current browser/E2E proof is closed, remaining product work includes:```textPENDING SEPARATE APPROVAL- global post-Planner Plan Edit- optional safe-boundary Phase Edit- optional safe-boundary Task/Step Edit- revision/supersession checkpoint strategy for those edits- full ContainerSandboxRunner execution parity- Conversation↔Run durable ownership/index only if existing provenance proves insufficient- real target upload/materialization if still absent```Do not begin these automatically.---46. Builder ReportingFinal implementation reports must include:ChangedCommands RunResultEvidenceDependency DeltaBrowser Contract ChangesRuntime Contract ChangesPersistence ChangesFiles CreatedFiles ModifiedFiles RemovedStop Conditionbrowser/E2E proof statusDo not hide missing runtime capability behind frontend mock behavior.---47. Final Preserve StatementOneShot is a governed AI engineering application.Its visible product relationship is:```textLEFTselected target projectCENTERconversation with OneShotRIGHTcurrent Job and Job History```with narrow application rails on both sides.The human experience remains:```textDescribe Intent→ Research→ Research Review→ Accept→ Prepare Work→ Refine→ Check Gaps→ Evaluate→ Validate→ Build Ready→ Confirm Build→ Build→ Sandbox→ Verify→ Final Result```The runtime remains authoritative.The UI is a synchronized projection and human control surface.Visual references may improve presentation, but they may never redesign workflow, state ownership, layout ownership, or runtime truth.